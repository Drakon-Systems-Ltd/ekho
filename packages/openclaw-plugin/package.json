{
  "name": "@drakon-systems/ekho-openclaw-plugin",
  "version": "0.3.0",
  "description": "OpenClaw plugin that connects agents to an Ekho relay so they can message and coordinate with the rest of the fleet.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "node build.mjs && tsc -p tsconfig.json --emitDeclarationOnly",
    "plugin:build": "npm run build && openclaw plugins build --entry ./dist/index.js",
    "plugin:validate": "npm run build && openclaw plugins validate --entry ./dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "@drakon-systems/ekho-sdk": "*",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.7",
    "typebox": "^1.1.38"
  },
  "publishConfig": {
    "access": "public"
  }
}
